# -*- coding: utf-8 -*-
"""
Каталог инструментов для planner (сжатая выжимка; полная правда — USAGE.md / USAGE-AGENT.md).
Имена инструментов должны совпадать с whitelist в services/tools.py.
"""
from __future__ import annotations


def _planner_cash_cities_subsnippet() -> str:
    """Строка для вставки в описание `get_cash_report`; порядок = ``cash_report._CASH_LOCATIONS``."""
    from cash_report import _CASH_LOCATIONS

    lines = "\n".join(f"  {i}. {t[0]} — `city_n`: {i}" for i, t in enumerate(_CASH_LOCATIONS, start=1))
    return (
        "  Номера городов (как `rates.py cash` без аргументов):\n"
        f"{lines}\n"
        "  Однозначный город из реплики → сразу **`city_n`**, один вызов; **не** делай отдельный шаг только за списком. "
        "`city_name` — нестандартное название или сомнение.\n\n"
    )


# Все команды rates.py — только с --readonly (кеш, без сети).
PLANNER_TOOLS_SNIPPET = (
    """
Обязательное поле JSON `think` (bool), одно на весь ответ:
- Главное правило: если запрос **однозначен** и в нём уже есть **все параметры**, нужные для **одного** вызова выбранного инструмента (одна команда `rates.py --readonly …`), ставь **`think: false`**. Размышление второй модели не нужно — пользователю отдают сырой вывод скрипта.
- `think: true` только когда без рассуждения/интерпретации не обойтись: неясно что спрашивают, нужен выбор из вариантов, «что выгоднее», сравнение сценариев, объяснение таблицы, ответ своими словами поверх цифр, или **не хватает параметров** и ты всё равно вызываешь инструмент с дефолтами — тогда может понадобиться пояснить.
- `think: false` — прямые просьбы дать отчёт/сводку/usdt/cash/exchange/rshb/calc с полным набором аргументов в тексте (или с явными дефолтами инструмента, без двусмысленности).

Цепочка `tool_steps` (нет **одной** команды, нужен сценарий из 2–5 вызовов `rates.py`):
- Для наличных по городу **не** используй два шага «список → отчёт», если номер города известен из списка в пункте `get_cash_report` ниже или из реплики (`city_n`). Пример цепочки только когда реально нужны **разные** команды (не дублирование cash).
- При **двух и более** шагах пользователю **всегда** отвечает вторая модель по запросу и по всем выводам; ставь **`think: true`**. При **одном** шаге при **`think: false`** пользователю может уйти сырой вывод `rates.py` без второй модели.
- `needs_tool: true`, если есть хотя бы один реальный вызов (в т.ч. только `tool_steps`).

Поле `arguments` (критично):
- Заполняй **только** из **текущего** сообщения пользователя (последняя реплика в диалоге). Не подставляй числа, города, валюты и т.д. из прошлых сообщений пользователя, если пользователь в этой реплике их **не повторил**.
- Общий запрос без параметров («курсы рсхб», «рсхб», «наличные», «сводка») → вызывай инструмент с **`arguments: {{}}`** (пустой объект) или только опциональные поля, явно сказанные в этой реплике — тогда сработают дефолты CLI.

Доступные инструменты (имя → смысл в терминах `rates.py --readonly`):

- `none` — ответ без данных из кеша (общий вопрос, уточнение). arguments: {}. needs_tool: false. think: обычно true, если нужен разговор; false только при тривиальном однозначном уточнении.

- `get_rates_summary` — сводка RUB→THB из кеша.
  arguments: `output_filter` (optional string): пусто | "travelask" | "ta" (пресет фильтра).
  needs_tool: true. think: false для прямой просьбы показать сводку; true если просят сравнить, выбрать лучший канал, объяснить разброс.

- `get_usdt_report` — отчёт USDT (как `rates.py --readonly usdt`).
  arguments: {}.
  needs_tool: true. think: false для «дай usdt / отчёт usdt»; true если нужна интерпретация или вывод из цифр.

- `get_rshb_report` — РСХБ / UnionPay (как `rates.py --readonly rshb`).
  arguments: `thb_amounts` (optional list of numbers), `atm_fee` (optional number).
  «курсы рсхб» / «рсхб» **без чисел в этой реплике** → **`arguments: {{}}`** (дефолт CLI: 30000 и комиссия 250). Числа в `thb_amounts` — только если пользователь написал их **в текущем сообщении**.
  needs_tool: true. think: false для однозначного запроса отчёта; true только при явном сравнении/интерпретации (см. общие правила think).

- `get_cash_report` — наличные (как `rates.py --readonly cash …`).
"""
    + _planner_cash_cities_subsnippet()
    + """  arguments: `city_n` (optional int — **предпочитай**, см. нумерованный список в этом пункте выше), `city_name` (optional — только если без `city_n`: тогда инструмент может вызвать `cash` без N для списка и сопоставить; **лишний раз список не нужен**, если город ясен по списку), `cash_fiat` или **`fiat`** (optional: "USD"|"EUR"|"CNY" — только эта валюта; **обязательно вместе с `city_n` или `city_name`**), `source` (optional: "banki"|"vbr"|"rbc"|"all"), `top_n` (optional int, по умолчанию 10).
  Пример: «курсы в москве» / «доллар в москве» → `{{"city_n": 1, "cash_fiat": "USD"}}` или `{{"city_n": 1, "fiat": "USD"}}` — **один** вызов.
  Если в реплике есть и номер, и название — приоритет у `city_n`. Только список городов → без `city_n` и без `city_name` (или `arguments: {{}}`).
  needs_tool: true. think: false, если город или номер ясен; true если нужен разбор источников или таблицы.
  Уточнения вроде только «доллар» без города: в `arguments` нельзя взять город из прошлых реплик при `CHAT_AGENT_PLANNER_USER_HISTORY_TURNS=0` — либо попроси пользователя повторить город в той же фразе, либо подними лимит истории в настройках.

- `get_exchange_report` — витрина TT Exchange (как `rates.py --readonly exchange`).
  arguments: `top_n` (optional int, по умолчанию 10), `exchange_fiat` (optional: "USD"|"EUR"|"CNY" — одна колонка и сортировка по этой валюте).
  needs_tool: true. think: false для «exchange / tt»; true если просят лучший филиал под критерий.

- `get_calc_comparison` — сравнение каналов RUB→THB через **промежуточную** валюту (как `rates.py --readonly calc <бюджет_руб> usd|eur|cny <курс>`).
  **Обязательны все три поля:** `budget_rub` (int, рубли), `fiat` — **только** строки `"usd"`, `"eur"` или `"cny"` (ни `thb`, ни «руб», ни другие коды), `rub_per_fiat` (number > 0) — **сколько ₽ за 1** USD/EUR/CNY (курс из фразы пользователя).
  Это не запрос «сколько бат за рубли» без промежуточной валюты: если пользователь просит только THB/RUB без USD/EUR/CNY и курса за единицу этой инвалюты — **не** вызывай calc; возьми `get_rates_summary` / `none` или попроси уточнить курс и валюту (usd/eur/cny).
  needs_tool: true. think: false только если в **этой** реплике явно есть бюджет в ₽, одна из трёх инвалют и число `rub_per_fiat`; иначе think: true или другой инструмент.

Если кеш пустой, stderr от rates.py объяснит; не выдумывай цифры — опирайся только на вывод инструмента.
"""
).strip()

# Имена, разрешённые в executor (включая ветку без вызова при needs_tool=false).
REGISTERED_TOOL_NAMES = frozenset(
    {
        "none",
        "get_rates_summary",
        "get_usdt_report",
        "get_rshb_report",
        "get_cash_report",
        "get_exchange_report",
        "get_calc_comparison",
    }
)
